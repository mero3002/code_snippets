#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define ld long double

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t;
    cin >> t;
    while (t--){
        int n;
        cin >> n;

        vector<int> vin(n);
        vector<int> vout(n * 4);
        for (int i = 0; i < n; i++) vin[i] = i + 1;

        for

        for (int item : vout) cout << item << ' ';


        cout << endl;
    }   

    return 0;
}