#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define ld long double

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    while (true){
        int pass;
        cin >> pass;
    
        if (pass == 1999) {
            cout << "Correct";
            break;
        }
        else cout << "Wrong" << endl;
    }

    return 0;
}